version: '3.9'
services:
  ai-engine:
    build: ./services/ai-engine
    ports:
      - "8000:8000"
    env_file:
      - .env
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: voltex
      POSTGRES_PASSWORD: voltex
      POSTGRES_DB: voltex
    ports:
      - "5432:5432"